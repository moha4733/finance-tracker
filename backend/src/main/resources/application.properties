# Docker Compose (spring-boot-docker-compose) — slå til og kør `docker compose up` hvis du bruger compose.yaml
spring.docker.compose.enabled=false

# Database - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/financetracker
spring.datasource.username=postgres
spring.datasource.password=dit_password

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=minHemmeligeNogleErMindst32TegnLang1234